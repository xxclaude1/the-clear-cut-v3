<style>
  #shopify-section-announcement-bar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 101;
    transition: transform 300ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  #shopify-section-announcement-bar.is-hidden {
    transform: translateY(-100%);
  }

  .announcement-bar {
    display: flex;
    align-items: center;
    justify-content: center;
    height: var(--announcement-height);
    background-color: var(--color-announcement-bg);
    color: var(--color-announcement-text);
    overflow: hidden;
  }

  .announcement-bar__messages {
    position: relative;
    text-align: center;
    width: 100%;
    height: 100%;
  }

  .announcement-bar__message {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-mono);
    font-weight: 400;
    font-size: 12px;
    letter-spacing: 0.02em;
    opacity: 0;
    transition: opacity 400ms ease;
    padding: 0 40px;
  }

  @media (max-width: 768px) {
    .announcement-bar__message {
      font-size: 11px;
    }
  }

  .announcement-bar__message.is-active {
    opacity: 1;
  }

  .announcement-bar__nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    color: var(--color-announcement-text);
    cursor: pointer;
    padding: 8px;
    z-index: 2;
    opacity: 0.7;
    transition: opacity var(--duration-fast) ease;
  }

  .announcement-bar__nav:hover {
    opacity: 1;
  }

  .announcement-bar__prev {
    left: 12px;
  }

  .announcement-bar__next {
    right: 12px;
  }

  .announcement-bar__nav svg {
    width: 10px;
    height: 10px;
  }
</style>

<div class="announcement-bar" role="region" aria-label="Announcements">
  <button class="announcement-bar__nav announcement-bar__prev" aria-label="Previous announcement">
    <svg viewBox="0 0 10 10" fill="none" stroke="currentColor" stroke-width="1.5">
      <polyline points="7,1 3,5 7,9"></polyline>
    </svg>
  </button>

  <div class="announcement-bar__messages">
    {%- if section.blocks.size > 0 -%}
      {%- for block in section.blocks -%}
        <div class="announcement-bar__message{% if forloop.first %} is-active{% endif %}">
          {{ block.settings.text }}
        </div>
      {%- endfor -%}
    {%- else -%}
      <div class="announcement-bar__message is-active">Free Shipping On All Orders $200+</div>
      <div class="announcement-bar__message">30-Day Returns On All Orders</div>
      <div class="announcement-bar__message">Lifetime Warranty Included</div>
    {%- endif -%}
  </div>

  <button class="announcement-bar__nav announcement-bar__next" aria-label="Next announcement">
    <svg viewBox="0 0 10 10" fill="none" stroke="currentColor" stroke-width="1.5">
      <polyline points="3,1 7,5 3,9"></polyline>
    </svg>
  </button>
</div>

<script>
  (function() {
    var lastY = 0;
    var bar = document.getElementById('shopify-section-announcement-bar');
    var header = document.getElementById('shopify-section-header');
    var threshold = 10;

    window.addEventListener('scroll', function() {
      var y = window.scrollY;
      if (y < 5) {
        bar.classList.remove('is-hidden');
        header.classList.remove('bar-hidden');
      } else if (y > lastY + threshold) {
        bar.classList.add('is-hidden');
        header.classList.add('bar-hidden');
      } else if (y < lastY - threshold) {
        bar.classList.remove('is-hidden');
        header.classList.remove('bar-hidden');
      }
      lastY = y;
    }, { passive: true });
  })();
</script>

{% schema %}
{
  "name": "Announcement Bar",
  "settings": [],
  "blocks": [
    {
      "type": "message",
      "name": "Message",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Message text",
          "default": "Free Shipping On All Orders $200+"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Announcement Bar",
      "blocks": [
        { "type": "message", "settings": { "text": "Free Shipping On All Orders $200+" } },
        { "type": "message", "settings": { "text": "30-Day Returns On All Orders" } },
        { "type": "message", "settings": { "text": "Lifetime Warranty Included" } }
      ]
    }
  ]
}
{% endschema %}
